       program arith
       real::a,b
  
       print *, 'Input two numbers'
       read *,a,b
       print * , 'Hello World'

       sum=a+b
       prod=a*b
       quotient=a/b

       print *,'sum is ', sum, ' product is ', prod, 'quotient is ',quotient
 
       
       end
