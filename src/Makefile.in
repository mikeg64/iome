# List of subdirectories
SUBDIRS=iome tools


all: libs tools


libs:
	@set -e; cd iome; make -f Makefile install

tools:
	@set -e; cd tools; make -f Makefile install

clean :
	@set -e; for i in $(SUBDIRS); do (cd $$i; make -f Makefile clean) || exit 1; done


